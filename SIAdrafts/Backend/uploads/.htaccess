# Uploaded files must only be served through download_attachment.php,
# which checks that the requester is part of the conversation.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>