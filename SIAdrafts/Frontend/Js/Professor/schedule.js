document.addEventListener('DOMContentLoaded', function () {
  // Weekly schedule is server-rendered and read-only for professors —
  // nothing to wire up client-side yet, kept for parity with other pages
  // and as the place to add interactivity later (e.g. print/export).
});
